"""
Python for Image and Video Processing with OpenCV
"""
import cv2
import glob

images=glob.glob(".jpg")

img=cv2.imread("galaxy.jpg", 0)
print(type(img))
print(img.shape)
print(img.ndim)

resize_image=cv2.resize(img,(1000, 500)) #number of pixels
cv2.imshow("Galaxy", img)
cv.waitKey(0)
cv2.destroyAllWindows()
cv2.imwrite("resized"+image,re)

'''
Disclosure: I decided to skip the Face Detection project #7.
I see no use for it at any point in my career path.
'''
